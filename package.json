{
  "name": "vast-parser",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "rm -rf test/tmp && mkdir test/tmp && mocha --ui=bdd --reporter=list --bail",
    "trace": "cat test/tmp/*.log | bunyan --output short"
  },
  "dependencies": {
    "bunyan": "^1.3.3",
    "lodash": "^3.2.0",
    "xmldom": "^0.1.19"
  },
  "devDependencies": {
    "mocha": "^2.1.0",
    "pem": "^1.5.0"
  }
}
